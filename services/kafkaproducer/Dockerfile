# Start with a base image containing JDK 17
FROM openjdk:17-jdk-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the Maven build file first to leverage Docker cache
COPY pom.xml .

# Download all dependencies (Docker layer caching)
RUN mvn dependency:go-offline

# Copy the rest of the project files into the container
COPY src ./src

# Package the application as a jar using Maven
RUN mvn clean package -DskipTests

# Expose port 8080 for external communication
EXPOSE 8080

# Run the jar file generated by Maven (replace the jar name if different)
ENTRYPOINT ["java", "-jar", "target/kafkaproducer-0.0.1-SNAPSHOT.jar"]
